/*
void login(){
    sendMsg("login");
    char userID[20]=ui->userID->Text();
    char passWord[20]=ui->passWord->Text();
    sendMsg(userID);
    sendMsg(passWord);
    if(recvMsg().equals("correct")){
        goto main ui;
    }
    else{
        show warning window "not correct"
    }
}
*/
